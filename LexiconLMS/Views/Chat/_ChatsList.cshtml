@model LexiconLMS.ViewModels.ChatsListViewModel
        @foreach (var item in Model.Chats)
        {
            <input type="button" value="@item.ChatName" onclick="showMessages(event,@item.ChatId)" class="chatlinks"  />
        }
<script>
    var showMessages = function (chatId) {
        content = content.replace("jsvar", "");
        var content = "@Html.Action("Messages", "Chat", new {id = "jsvar"})";
        $('.main').empty();
        $('.main').append(content);
    }

    
</script>

